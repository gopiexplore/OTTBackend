<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Movie</title>
</head>
<body>
    <h2>Delete Movie</h2>
    <a href="/">Go to Dashboard</a>
    {{#each movies}}
    <div class="movieDetailsContainer">
        <div class="posterContainer">
            <img src="https://image.tmdb.org/t/p/original/{{this.posterPath}}" alt="Movie Poster">
        </div>
        <div class="detailsTextContainer">
            <h2>{{this.title}}</h2>
            <p style="font-size: 0.8em; margin-bottom:5px">{{this.overview}}</p>
            <p style="font-size: 0.8em; font-weight:bold">{{this.releaseDate}}</p>
            <form action="/delete-movie/{{this._id}}" method="post">
            <button class="delete-btn" type="submit">Delete</button>
            
            </form>
        </div>
    </div>
    {{/each}}
</body>
<style>
    .movieDetailsContainer{
        padding: 10px;
        display: flex;
        align-items: center;
        width: 50%;
        margin: 0 auto;
    }
    .posterContainer{
        margin-right: 20px;
    }
    .posterContainer img{
        width: 100px;
        height: auto;
    }
    .detailsTextContainer{
        text-align: left;
    }
    .detailsTextContainer h2{
        margin-bottom: 10px;
    }
    a{
        text-decoration: none;
        color: black;
    }
</style>
</html>